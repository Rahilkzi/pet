<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Pets</title>

</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/browse_pet.css">
    <title>Header Example</title>

</head>
<body>
    <%- include('components/navbar') %>
</body>
</html>


    <div class="header">Browse Pets</div>

    <div class="buttons">
        <a href="#dogs" class="btn">Dogs</a>
        <a href="#cats" class="btn">Cats</a>
    
    </div>

   <div class="pet-listings">
        <!-- Dogs Section -->
        <h2 id="dogs" style="text-align: center;">Dogs</h2>
        <div class="pet-grid">
            <% pets.filter(pet => pet.category.toLowerCase() === 'dog').slice(0, 3).forEach(pet => { %>
                <div class="pet-card">
                    <img src="<%= pet.image_url || '/images/default-dog.jpg' %>" alt="<%= pet.name %>">
                    <h3><%= pet.name %></h3>
                    <p>Age: <%= pet.age %> <%= pet.age === 1 ? 'year' : 'years' %></p>
                    <p>Breed: <%= pet.breed %></p>
                    <p>Gender: <%= pet.gender || 'Unknown' %></p>
                    <a href="/adopt/<%= pet.id %>" class="adopt-btn">Adopt Me</a>
                </div>
            <% }); %>
        </div>
        
        <div class="pet-grid">
            <% pets.filter(pet => pet.category.toLowerCase() === 'dog').slice(3, 6).forEach(pet => { %>
                <div class="pet-card">
                    <img src="<%= pet.image_url || '/images/default-dog.jpg' %>" alt="<%= pet.name %>">
                    <h3><%= pet.name %></h3>
                    <p>Age: <%= pet.age %> <%= pet.age === 1 ? 'year' : 'years' %></p>
                    <p>Breed: <%= pet.breed %></p>
                    <p>Gender: <%= pet.gender || 'Unknown' %></p>
                    <a href="/adopt/<%= pet.id %>" class="adopt-btn">Adopt Me</a>
                </div>
            <% }); %>
        </div>

        <!-- Cats Section -->
        <h2 id="cats" style="text-align: center;">Cats</h2>
        <div class="pet-grid">
            <% pets.filter(pet => pet.category.toLowerCase() === 'cat').slice(0, 3).forEach(pet => { %>
                <div class="pet-card">
                    <img src="<%= pet.image_url || '/images/default-cat.jpg' %>" alt="<%= pet.name %>">
                    <h3><%= pet.name %></h3>
                    <p>Age: <%= pet.age %> <%= pet.age === 1 ? 'year' : 'years' %></p>
                    <p>Breed: <%= pet.breed %></p>
                    <p>Gender: <%= pet.gender || 'Unknown' %></p>
                    <a href="/adopt/<%= pet.id %>" class="adopt-btn">Adopt Me</a>
                </div>
            <% }); %>
        </div>
        
        <div class="pet-grid">
            <% pets.filter(pet => pet.category.toLowerCase() === 'cat').slice(3, 6).forEach(pet => { %>
                <div class="pet-card">
                    <img src="<%= pet.image_url || '/images/default-cat.jpg' %>" alt="<%= pet.name %>">
                    <h3><%= pet.name %></h3>
                    <p>Age: <%= pet.age %> <%= pet.age === 1 ? 'year' : 'years' %></p>
                    <p>Breed: <%= pet.breed %></p>
                    <p>Gender: <%= pet.gender || 'Unknown' %></p>
                    <a href="/adopt/<%= pet.id %>" class="adopt-btn">Adopt Me</a>
                </div>
            <% }); %>
        </div>
    </div>
<script>
        document.addEventListener('DOMContentLoaded', function() {
            const filterButtons = document.querySelectorAll('.filter-btn');
            const petCards = document.querySelectorAll('.pet-card');
            const loadingSpinner = document.getElementById('loading-spinner');
            
            // Hide spinner when page loads (since we're using server-side rendering)
            if (loadingSpinner) {
                loadingSpinner.style.display = 'none';
            }
            
            // Filter functionality
            filterButtons.forEach(button => {
                button.addEventListener('click', () => {
                    // Update active button
                    filterButtons.forEach(btn => btn.classList.remove('active'));
                    button.classList.add('active');
                    
                    const category = button.dataset.category;
                    
                    // Filter pets
                    petCards.forEach(card => {
                        if (category === 'all' || card.dataset.category === category) {
                            card.style.display = 'block';
                        } else {
                            card.style.display = 'none';
                        }
                    });
                });
            });
        });
    </script>
    <script>
        // Add any client-side functionality here if needed
        document.querySelectorAll('.btn').forEach(btn => {
            btn.addEventListener('click', () => {
                // Smooth scroll to section
                const targetId = btn.getAttribute('href');
                document.querySelector(targetId).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>

    <div class="adoption-info">
        <h2>Adoption Information</h2>
        <p>To adopt a pet, please contact us at:</p>
        <p>📞 <b>Phone:</b> 123-456-7890</p>
        <p>📧 <b>Email:</b> adoptioncenter@example.com</p>
    </div>
    
</body>
</html>
